bins = 256
metric = 'L2'
log_dir = 'work_dir'

analysis_weight = dict(bins=bins, is_pertensor=True, is_observer=True,
                       is_always=False, metric=metric, log_dir=log_dir)
analysis_feat = dict(bins=bins, is_pertensor=True, is_observer=True,
                     is_always=False, metric=metric, log_dir=log_dir)
